name: e2e-ci-smoke

on:
  pull_request:
    branches: ["main", "sprint-q-prompts"]
    paths:
      - "e2e/**"
      - "client/**"
      - ".github/workflows/**"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e:
    runs-on: ubuntu-22.04
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Playwright (Chromium + deps)
        run: npx playwright install --with-deps chromium

      # Build with the E2E shim baked in (belt & braces)
      - name: Build app (with E2E shim)
        env:
          VITE_E2E_SHIM: "1"
        run: npm run build

      # Serve the built app on 127.0.0.1:4173
      - name: Start preview server (Vite)
        run: |
          npx vite preview --port 4173 --host 127.0.0.1 --strictPort &
          npx wait-on http://127.0.0.1:4173

      # Run only @ci-tagged smoke tests; force runtime shim via query param
      - name: Run @ci smoke tests (trace always)
        env:
          CI_BASE_URL: http://127.0.0.1:4173/?shim=1
          SKIP_E2E_LOCAL: ""
        run: npx playwright test --project=chromium --grep "@ci" --reporter=line --trace on

      - name: Upload Playwright report (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report
          if-no-files-found: ignore

      - name: Upload traces (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: traces
          path: test-results/**/*.zip
          if-no-files-found: ignore