import * as React from 'react';
import App from '@/App'; // your real app entry
export default function AppRouter() {
  // If your real app already includes its own router, just render <App/>
  return <App />;
}
